import tank/state

import entity/health
import entity/money

import square

byte selected_tank

const array(Tank) tanks = [export_square_tank]

void init_tank() {
  init_money()
  ammonia_level = 200
  selected_tank = 0
  active_tank = export_square_tank
}

void render_tank(word nametable) {
  call(active_tank.render_func, nametable)
}